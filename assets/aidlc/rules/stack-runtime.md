# Stack runtime — concrete commands (AI-DLC · project rule)

> Optional project-specific stack lifecycle/exec commands. Keep them idempotent and check-first, never tear down a shared running stack, and configure narrow test/lint commands in `.agents/config.json` or local profiles.
