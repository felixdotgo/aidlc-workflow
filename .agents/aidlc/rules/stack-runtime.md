# Stack runtime — concrete commands (AI-DLC · project rule)

> Project-specific stack lifecycle/exec commands. The **generic principle** (idempotent, check-first, never tear down a shared running stack) lives in `.agents/aidlc/conventions.md §7`; this file holds the concrete commands for THIS repo. Per-submodule test/lint commands live in `.agents/state/repo-map.md`.
