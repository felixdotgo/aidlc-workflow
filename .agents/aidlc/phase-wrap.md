# AI-DLC · Wrap

Precondition: the user approved G2 and canonical state contains passing G2 approval evidence.

1. Commit only when the user requested it. Follow the project's configured delivery policy; kernel does not assume submodules, branches, PR provider, or issue tracker.
2. Capture durable corrections as lessons. Promote a rule only with explicit user approval, preferring executable enforcement over prose.
3. Record delivery evidence and residual risk.
4. Transition the task to `done`, archive it in canonical state, and regenerate views.

Post-condition: reported commits/integrations match reality, task state is archived, generated BOARD has no completed row, and no local rule was silently admitted.
